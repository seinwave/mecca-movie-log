<div>
  <h1>Ratings</h1>
  <div>
    <%= @ratings.each do |rating| %>
    <div>
      <h2><%= rating.movie.title %></h2>
      <p><%= render_rating(rating) %></p>
    </div>
    <% end %>
  </div>
</div>
