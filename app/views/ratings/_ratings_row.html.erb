<div>
  <ul>
    <% movie_groups = group_ratings_by_movie(ratings, @rebecca, @matt) %> <%
    movie_groups.each do | group | %>
    <h3><%= group[:title] %></h3>
    <li>
      <%= "Rebecca: #{render_rating(group[:user1_rating])}" %> <%= "Matt:
      #{render_rating(group[:user2_rating])}" %>
    </li>
    <% end %>
  </ul>
</div>
